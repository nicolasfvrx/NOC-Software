[server]
listen = "0.0.0.0"
port = 8080
# Empty = no authentication (LAN use).
# If set, the API requires: Authorization: Bearer <token>
api_token = ""

[data]
file = "kiosks.json"
commands_file = "commands.json"
